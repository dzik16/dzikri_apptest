import { StyleSheet } from 'react-native';
import { COLORS, FONTS } from '../../themes';

export default StyleSheet.create({
  cardProductWrapper: {
    marginTop: 10,
    flex: 1,
    flexWrap: 'wrap',
    marginHorizontal: 15,
  },

  imageContact: {
    borderRadius: 15,
    height: 100,
    width: 100,
    marginRight: 16,
    marginBottom: 5
  },
});
